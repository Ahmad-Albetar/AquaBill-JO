# دليل المساهمة (Contributing)

شكراً لاهتمامك بتحسين AquaBill JO! هذا المشروع بسيط عمداً (HTML/CSS/JS خام بدون أي إطار عمل)، والمساهمة فيه لا تحتاج إعداداً معقداً.

## قبل البدء

اقرأ أولاً:
- `README.md` — لفهم بنية المشروع وطريقة التشغيل المحلي.
- `ROADMAP.md` — لمعرفة الأفكار المخطط لها مسبقاً (تجنّباً لتكرار العمل).
- `CHANGELOG.md` — لمعرفة آخر التحديثات.

## قواعد أساسية غير قابلة للتفاوض

عند تعديل منطق الحساب بـ `script.js`، **يجب ألا تتغيّر نتيجة أي حساب موجود**. هذه الأداة تُستخدم فعلياً من أسر تعتمد على أرقامها، فأي خطأ حسابي يضر بثقة المستخدمين. إن أردت تعديل شريحة سعرية، غيّر القيم بـ `config.js` فقط، لا تلمس صيغة الحساب بـ `costFor()`.

## خطوات المساهمة

1. Fork للمستودع.
2. أنشئ فرعاً جديداً باسم واضح: `git checkout -b fix/typo-in-tips` أو `feature/english-support`.
3. عدّل الكود محلياً، واختبره يدوياً بفتح `index.html` بالمتصفح (أو عبر خادم محلي بسيط، راجع README).
4. تأكد من:
   - عدم ظهور أي خطأ بوحدة تحكم المتصفح (Console).
   - عمل الحاسبة بنفس الدقة (جرّب استهلاك 15، 17، 18 م³ وقارن بالنتائج الموثقة بـ README).
   - عدم كسر الوضع الداكن أو زر التبديل.
5. أضف سطراً بـ `CHANGELOG.md` تحت قسم "غير منشور" يصف تعديلك.
6. أرسل Pull Request مع وصف واضح لماذا التعديل مطلوب.

## الإبلاغ عن مشكلة (Issue)

اذكر:
- ما الذي توقعته أن يحدث؟
- ما الذي حدث فعلياً؟
- خطوات إعادة إنتاج المشكلة (مثلاً: "أدخلت 25 بحقل الاستهلاك، ثم...").
- نوع الجهاز والمتصفح.

## أسلوب الكود

- تعليقات بالعربية (يستهدف المشروع مستخدمين ومطورين أردنيين/عرب بالدرجة الأولى).
- Arabic RTL بكل الواجهة — لا تُدخل نصوصاً بالإنجليزية بالواجهة إلا لاسم العلامة التجارية "AquaBill JO" فقط.
- التزم بنفس هيكلة الأقسام الموجودة بـ `style.css` و`script.js` (مرقّمة ومُعلَّق عليها).
